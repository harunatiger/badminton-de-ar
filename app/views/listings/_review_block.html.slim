/* listings#review_block */
#review-block-container
  #review-block
    .row-space-4.row-space-top-8
      h3.row-space-2
        | Review
        ' 
        small.text-muted
          = reviewed_count
        .star-rating
          = render partial: 'shared/stars', locals: { rate: listing.ave_total }
    .review-content
      = render partial: 'review_item', locals: { reviews: reviews, reviewed_count: reviewed_count }
      /= paginate reviews
