$('#favorites_list').html($("<%=j render partial: 'favorites_list', locals: { bookmarked_histories: @bookmarked_histories.first(@count), all_count: @bookmarked_histories.count} %>").hide().fadeIn('slow'));
$('#listing-image-loading').modal('hide');