Hello, <%= @to_user_name %>


Let's fill in a review of <%= @guest_user_name %> who was participated <%= @listing.title %> (<%= @reservation.schedule %>)

You can fill in the review from the following link.

<%= new_reservation_reviews_review_replies_url(@reservation.id) %>

When you fill in the review, it will be published i profile page.


--
Huber.