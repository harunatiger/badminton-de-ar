Hello, <%= @to_user_name %>


Would you like to write a review for <%= @guest_user_name %> from your tour <%= @listing.title %> (<%= @reservation.schedule %>)?

You can write a review by clicking the following link.

<%= new_reservation_reviews_review_replies_url(@reservation.id) %>

After both the guide and guest finish writing reviews, each review will be published on their own page.


--
Huber.