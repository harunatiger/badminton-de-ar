Hello,  <%= @to_user_name %>


Would you like to write a review for <%= @listing.title %> (<%= @reservation.schedule %>) planned by <%= @host_user_name %>?

You can write a review by clicking the following link.

<%= for_guide_reservation_reviews_url(@reservation.id) %>

After both the guide and guest finish writing reviews, each review will be published on their own page.



You have <%= Settings.review.expiration_date %> days to write a review for this tour.
--
Huber.					