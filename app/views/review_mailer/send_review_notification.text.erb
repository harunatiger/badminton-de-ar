<%= @to_user_name %>
さん、こんにちは！

<%= @host_user_name %>さんのガイド、<%= @listing.title %> (<%= @reservation.schedule %>)
へのレビューの記入をしませんか？

以下のリンクから、レビューの記入ができます。

<%= new_reservation_reviews_url(@reservation.id) %>

レビューを記入すると、ガイドの方より返信がきます。
返信が来ると、レビューは公開されます。

なおレビューの書き込みは、<%= Settings.review.expiration_date %> 日間有効です。

--
Huber.
