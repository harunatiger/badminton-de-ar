= simple_form_for(:search, url: search_path, html: {class: "trip-form", method: 'get', id: 'search_form'}) do |f|
  = f.hidden_field :latitude, value: @conditions['latitude']
  = f.hidden_field :longitude, value: @conditions['longitude']
  = f.hidden_field :sort_by, value: @conditions['sort_by']
  = f.hidden_field :schedule, value: @conditions['schedule']
  = f.hidden_field :num_of_people, value: @conditions['num_of_people']
  = f.hidden_field :duration, value: @conditions['duration']
  = f.hidden_field :language_ids, value: @conditions['language_ids']
  .map-search-container
    .sidebar
      .search-trigger--sp.show--sp.page-container
        .row.row-condensed
          .col-sm-8
            input#location-search-sp[type="text" placeholder="location" value=@conditions['location'] name="search[location]"]
            i.fa.fa-search.fa-brand
          .col-sm-4
            = link_to "FILTERS", "#", class: "js-small-filter-show btn btn-block btn-primary"

      = render partial: 'filter', locals: { hit_count: @hit_count, conditions: @conditions, f: f }
      .sidebar-header-placeholder.show--sp
      - if @results.blank?
        .no-listings-block
          div
            p
              | 条件に該当がないようです。
              br
              | 条件を変えて検索してみましょう。
      - else
        = render partial: 'result_block', locals: { results: @results, conditions: @conditions }
    .map.hide--sp
      - if @results.blank?
        .no-listings-map
          div
            i.fa.fa-map-marker
            p
              | 該当がありません。
      - else
        #map.map-canvas
        .map-refresh-controls.Google
          a.map-manual-refresh.btn.btn-primary.hide
            | 再検索
            i.fa.fa-refresh.fa-space-left
          /.card.map-auto-refresh.hide
            label.checkbox
              input.map-auto-refresh-checkbox checked="checked" type="checkbox"
              small マップを動かして自動検索
